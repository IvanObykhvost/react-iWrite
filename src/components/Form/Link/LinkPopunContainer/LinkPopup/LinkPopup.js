import React from 'react';
import PopupContainer from '../../PopupContainer/PopupContainer';

export default function LinkPopup({isOpen, onClick}) {
    return (
        <div> 
            <link
                onClick={e => this.openModal()}
            >
                {this.props.contentLabel}
            </link>

            {
                state.isOpen ?
                    <PopupContainer

                    />
                    :
                    null
            }
        </div>
        
    )
}