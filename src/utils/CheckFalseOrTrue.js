// import PropTypes from 'prop-types';

export default function CheckFalseOrTrue(object) {
    if(object){
        return true
    }
    return false;
}

// CheckFalseOrTrue.propTypes = {
//     object: PropTypes.object
// }